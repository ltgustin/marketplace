{
  "buildCommand": "npm run build",
  "outputDirectory": "client/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "server/api/:path*"
    },
    {
      "source": "/admin/:path*",
      "destination": "server/admin/:path*"
    }
  ]
} 